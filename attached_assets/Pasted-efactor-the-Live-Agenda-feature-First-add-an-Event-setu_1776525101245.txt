efactor the Live Agenda feature. First, add an Event setup layer — before adding sessions, the organiser creates an event with just three fields: event name, event date, and event sector (dropdown: Sustainability, Finance, Supply Chain, Healthcare, Legal, Insurance, AI). Store this in a new Supabase table called events with fields: id (uuid primary key), event_name (text), event_date (text), event_sector (text), created_by (uuid references auth.users), created_at (timestamptz default now()). Run this SQL: CREATE TABLE IF NOT EXISTS events (id UUID PRIMARY KEY DEFAULT gen_random_uuid(), event_name TEXT, event_date TEXT, event_sector TEXT, created_by UUID REFERENCES auth.users(id), created_at TIMESTAMPTZ DEFAULT NOW()); ALTER TABLE events ENABLE ROW LEVEL SECURITY; CREATE POLICY \"Users can manage their events\" ON events FOR ALL USING (auth.uid() IS NOT NULL); Add an event_id foreign key to the sessions table: ALTER TABLE sessions ADD COLUMN IF NOT EXISTS event_id UUID REFERENCES events(id); The Live Agenda page should show an event selector at the top — organiser picks which event they are working on. If no events exist, show a 'Create your first event' prompt. Second, simplify the Add Session modal to these fields only: session title (text input), format (dropdown: Keynote, Panel, Roundtable, Workshop, Fireside), session brief (textarea, placeholder: 'Add 2-3 bullet points on what this session covers'), speakers (textarea, placeholder: 'e.g. Sarah Jones, CSO Unilever'). Remove audience and sponsor fit fields entirely. When a session is saved, automatically call the Claude API to generate: audience description (2 sentences on who this session is built for based on the session title, format, brief and event sector) and sponsor fit (2 sentences on which sponsor types belong in this session and why). Store both in the sessions table. Display the AI-generated audience and sponsor fit in the expanded session card — label them clearly as AI-generated with a small badge. Organiser can regenerate them with a refresh button if needed.